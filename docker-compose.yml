version: '3.9'

services:
  backend:
    build:
      context: ./backend
    image: data-agent-backend:latest
    container_name: data-agent-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - /tmp:/tmp
    restart: unless-stopped
